# 03 answer

In `.env`:

```dotenv
QUEUE_CONNECTION=database
```

In `device:check`, replace the exception with:

```php
CheckDeviceCommunication::dispatch($device->id);
```

The command loads the ID, dispatch stores the job, and the separate worker runs `handle()`. A command is only our convenient entry point; a controller can dispatch the same job.
